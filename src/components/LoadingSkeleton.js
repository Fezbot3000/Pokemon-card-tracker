import React from 'react';

// The invisible loading component maintains all the loading state 
// functionality but doesn't display any UI
const LoadingSkeleton = () => {
  // Return an empty fragment that takes no space
  return <></>;
};

export default LoadingSkeleton; 